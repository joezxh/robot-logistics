# Dependencies for the VLA fine-tuning workspace.
#
# NOTE: nothing here is installed automatically and no model weights are
# downloaded by this repository. Install deliberately, on a machine with a GPU:
#
#     pip install -r vla-training/requirements.txt
#
# Install the CUDA build of torch that matches your driver first; the generic
# PyPI wheel is CPU-only on some platforms.

# --- core training stack ---
torch>=2.1,<3.0
transformers>=4.40,<5.0
accelerate>=0.28,<2.0

# Parameter-efficient fine-tuning (LoRA).
peft>=0.10,<1.0

# --- data ---
datasets>=2.18,<4.0
numpy>=1.24,<3.0
Pillow>=10.0            # image decoding
pyyaml>=6.0             # config loading

# --- experiment tracking (pick one; tensorboard is the default) ---
tensorboard>=2.15
# wandb>=0.16

# --- optional: 4-bit QLoRA, roughly halves VRAM again ---
# bitsandbytes>=0.43

# --- data collection off the live MQTT bus ---
paho-mqtt>=1.6,<3.0

# --- development ---
pytest>=7.4
