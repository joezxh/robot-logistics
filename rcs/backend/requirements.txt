# RCS backend runtime dependencies
# Python >= 3.11 (see pyproject.toml)
# Install with:  pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.35.0
pydantic>=2.13.0
pydantic-settings>=2.12.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
numpy>=1.26.0
python-multipart>=0.0.18

# DXF import/export support (used by rcs/api/topology_export.py)
ezdxf>=1.1.0

# Contracts package (shared/python/robot_contracts, installable as robot-contracts)
robot-contracts>=1.1.0

# --- System administration (rcs/sysadmin) ---
# Password hashing and JWT signing for the console auth flow.
bcrypt>=4.0.0
PyJWT>=2.8.0

# --- Dev / test dependencies (uncomment if needed for tests) ---
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
